import { Text, View } from "react-native";

const Deployment = () => {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text>Deployment screen</Text>
        </View>
    );
}
export default Deployment;