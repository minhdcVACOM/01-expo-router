import { TextHeader } from "@/components/textHeader";
import { Text, View } from "react-native";

const MenuPublicWork = () => {
    return (
        <View style={{ flex: 1, alignItems: "center" }}>
            <TextHeader title="MENU CHUNG" />
        </View>
    );
}

export default MenuPublicWork;